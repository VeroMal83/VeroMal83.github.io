cd ~/VeroMal83.github.io

# Set your Git identity (once)
git config --global user.name "Malik Vero"
git config --global user.email "your@email.com"  # Use your GitHub email

# Initialize the repo and create a branch
git init
git branch -M main

# Stage and commit
git add --all
git commit -m "Initial commit"

# Add remote
git remote add origin https://github.com/VeroMal83/VeroMal83.github.io.git

# Push to GitHub
git push -u origin main

Hardening:
touch .nojekyll
git add .nojekyll
git commit -m "Disable Jekyll"
git push


🔐 Fix: Use a Personal Access Token (PAT)

You need to replace your GitHub password with a token when pushing.
✅ Step-by-Step:

    Generate a GitHub PAT (Personal Access Token):

        Go to https://github.com/settings/tokens

        Click "Generate new token"

            Use classic token (not fine-grained for now)

            Set scope to:

                ✅ repo

            Name it something like “VeroMal Git Push”

            Set expiration (e.g. 90 days, or no expiration)

        Copy the token — you won’t see it again.

Push again, but use the token as the password:
git push -u origin main

git config --global credential.helper store

